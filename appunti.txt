Da installare Virtual Box a casa: si inizia con Linux!!!!!!!!!!!!!

GUI graphic user interface Windows Apple Linux <--->

Internet -> Gateway -> Proxy

Notebook LM -> il ritorno

Stream -> di Google

BIOS -> Basic Input Output System

    INTEL -v da abilitare se la virtual machine non va 

https://curi.live/

UNIX da eviscerare!!!!

ipconfig -> la lettera A delle reti
    STORIA DEL PING => sonar dei sottomarini
ping + indirizzo IP => bussa alle porte di un dispositivo, se è aperte entra se no viene bloccato il ping

Mac Adress: Indirizzo fisico della scheda di rete

    ipconfig /all -> infos

    IPV4 = mac adress -> il più usato

ifconfig -> ci fa vedere la rete
    - Nome dell'interfaccia (es. eth0, wlan0, lo)
    - Indirizzo IP assegnato all'interfaccia
    - MAC address (indirizzo hardware)
    - Stato dell'interfaccia (attiva/inattiva, UP/DOWN)
    - Statistiche sui pacchetti trasmessi/ricevuti, errori, collisioni

IDONTCARE ABOUT COOKIES

Comandi: 
    mkdir
    ls -al -> (admin, long)
    cd (change directory)
    pwd(where I am)
    rm per file (rimuove)
    rm -r per directory 1. coincidenza
    mv (muove e rinomina)
    mv /nomedellacartella ../Music (nome della cartella in cui la voglio mettere)per esempio 
    cp -> copia ( -r per directory) 1. coincidenza
    touch -> crea file
    VI shortcut editor
    nano text editor -> si usa facendo nano e nome del file
    cat -> legge il file
    find trova una cartella -name
 

distrosea.com -> ti fa provare una distro di linux
leonardo ai per le foto
aistudio per le mappe concettuali
lovable.dev
mcp model context protocol for ai per migliorare l'uso del pc (ex. Claude)
Zapier


